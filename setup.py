from setuptools import setup, find_packages

setup(
    name="ASFT App",
    version="0.1",
    description="Python app for managing ASFT reports",
    author='Lucas Ariel Tkacz',
    packages=find_packages(),
)

# pip install -e .